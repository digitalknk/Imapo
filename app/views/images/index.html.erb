<% @images.each do |image| %>
<div>
  <div><%= link_to image_tag(image.image_url(:thumb)), image %></div>
    <span><%= link_to 'Edit', edit_image_path(image) %> | <%= link_to 'Destroy', image, method: :delete, data: { confirm: 'Are you sure?' } %></span>
</div>
<% end %>
<br />
